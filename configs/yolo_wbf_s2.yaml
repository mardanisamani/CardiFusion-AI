# WBF ensemble member C — seed 2. PURE multi-seed: identical to s0/s1 except the
# seed (different weight init + augmentation order). AI-assisted config.
name: yolov8_wbf_s2
seed: 2
model: yolov8m.pt
data_yaml: data/yolo/data.yaml
epochs: 100
imgsz: 640
batch: 16
optimizer: SGD
lr0: 0.01
lrf: 0.01
patience: 30
close_mosaic: 10
project: runs/wbf
weights_path: weights/wbf/yolov8_s2.pt
